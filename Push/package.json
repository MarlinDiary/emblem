{
  "name": "emblem-push",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:security && npm run test:worker",
    "test:security": "node --test test/security.test.mjs",
    "test:worker": "vitest run",
    "deploy:dry": "wrangler deploy --dry-run --outdir .wrangler-dry"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.8",
    "vitest": "4.1.11",
    "wrangler": "4.131.1"
  }
}
